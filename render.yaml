services:
  - type: web
    name: regreen-case-demo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: EXTRACAO_MODEL
        value: claude-sonnet-4-6
